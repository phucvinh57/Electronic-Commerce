export enum ProductSortCriterion {
    BEST_SELLER = "BEST_SELLER",
    ON_SALE = "ON_SALE",
    NEW_ARRIVAL = "NEW_ARRIVAL",
    NONE = "NONE"
}

export enum SortOrder {
    ASC = "asc",
    DESC = "desc"
}
